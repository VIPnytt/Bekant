[project]
name = "bekant"
version = "1.0.0"
readme = { file = "README.md", content-type = "text/markdown" }
classifiers = ["Private :: Do Not Upload"]
requires-python = ">=3.11"

[dependency-groups]
dev = ["platformio==6.1.19"]
